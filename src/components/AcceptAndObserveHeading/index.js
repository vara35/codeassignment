import {AcceptHeading} from './styledComponents'

const AcceptAndObserveHeading = props => {
  const {text} = props
  return <AcceptHeading text={text}>{text}</AcceptHeading>
}
export default AcceptAndObserveHeading
